<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <!-- The above 3 meta tags *must* come first in the head; any other head content must come *after* these tags -->
    <link href='https://fonts.googleapis.com/css?family=Abel' rel='stylesheet' type='text/css'>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
    <title>DBXperiment</title>
  </head>
  <body>
    <header>
      <div class="logo">
        <a href="/"><label>dbXperiment</label></a>
      </div>
      <div class="navbar">
        <ul>
          <% if logged_in?%>
            <% if admin? %>
              <li class="nav_list">ADMIN</li>
            <% end %>
            <li class="nav_list"><a href="/users/show">Signed in as <%= current_user.first_name %> <%= current_user.last_name %></a></li>
            <li class="nav_list"><a href="/sessions/logout">Logout</a></li>
          <% else %>
          <li class="nav_list"><a href="/users/new">Register</a></li>
          <li class="nav_list"><a href="/sessions/login">Login</a></li>
          <% end %>
        </ul>
      </div>
    </header>
      <div class="container">
        <%= yield %>
      </div>
  </body>
</html>
